import React from 'react'
import './coidinfo.css'

export default function Coidinfo() {
    return (
        <div>
            
            <h1>working on issue with the validation where it wont validate hidden tabs in the form, possible solution need to be tested, call submit function everytime we change tabs, or add boolean as long as submit button is not clicked form should not validate (add addtional  boolean state default to false' when user clicks submit we will set this boolean to true.</h1>
            <h1>use accordion for KYC</h1>
        </div>

    )
}
